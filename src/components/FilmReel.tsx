'use client';

import React from "react";
import { BackgroundLines } from "@/components/ui/background-lines";

export default function FilmReel() {
  return (
    <section id="the-reel" className="w-full">
      <BackgroundLines className="w-full min-h-[90vh]" />
    </section>
  );
}
